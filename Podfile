# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Protecfer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Protecfer
   pod 'Alamofire', '~> 4.7'
   pod 'KeychainAccess'	
   pod 'Toast-Swift', '~> 3.0.1'
   pod 'PayPal-iOS-SDK'
   pod 'PusherSwift','~> 5.0'
  target 'ProtecferTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ProtecferUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
